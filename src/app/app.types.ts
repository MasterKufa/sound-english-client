export enum Paths {
  vocabulary = "/vocabulary",
  settings = "/settings",
  player = "/player",

  // path that does not match tabs must be below
  word = "/vocabulary/:id",
  root = "/",
  fileUpload = "/vocabulary/file-upload",
}
