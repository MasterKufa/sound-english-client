import { Lang, NewWord } from "shared/vocabulary.types";

export const DEFAULT_WORD: NewWord = {
  sourceWord: {
    lang: Lang.en,
    text: "",
  },
  targetWord: {
    lang: Lang.ru,
    text: "",
  },
};
